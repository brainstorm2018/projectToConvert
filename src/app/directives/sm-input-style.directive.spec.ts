import { SmInputStyleDirective } from './sm-input-style.directive';

describe('SmInputStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new SmInputStyleDirective();
    expect(directive).toBeTruthy();
  });
});
