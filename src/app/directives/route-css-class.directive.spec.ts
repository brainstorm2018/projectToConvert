import { RouteCssClassDirective } from './route-css-class.directive';

describe('RouteCssClassDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteCssClassDirective();
    expect(directive).toBeTruthy();
  });
});
