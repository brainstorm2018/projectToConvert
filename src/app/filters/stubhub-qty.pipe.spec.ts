import { StubhubQtyPipe } from './stubhub-qty.pipe';

describe('StubhubQtyPipe', () => {
  it('create an instance', () => {
    const pipe = new StubhubQtyPipe();
    expect(pipe).toBeTruthy();
  });
});
