import { StubhubPriceRangePipe } from './stubhub-price-range.pipe';

describe('StubhubPriceRangePipe', () => {
  it('create an instance', () => {
    const pipe = new StubhubPriceRangePipe();
    expect(pipe).toBeTruthy();
  });
});
